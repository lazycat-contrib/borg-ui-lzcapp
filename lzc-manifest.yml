ext_config:
  enable_document_access: true
  enable_media_access: true
application:
  subdomain: borg-ui
  multi_instance: true
  background_task: true
  public_path:
    - /
  upstreams:
    - location: /
      backend: http://borg-ui:8081/
services:
  borg-ui:
    # upstream: ainullcode/borg-ui:2.2.6
    image: registry.lazycat.cloud/czyt/ainullcode/borg-ui:258737122cf69b0e
    user: root
    binds:
      - /lzcapp/var/data:/data
      - /lzcapp/var/cache:/home/borg/.cache/borg
      - /lzcapp/document/{{.U.backup_dir_name}}:/local
    environment:
      - TZ={{.U.time_zone}} # Set your timezone
      - PUID=1000
      - PGID=1000
